# Sample Data

Place a short meeting audio file here (e.g. `sample_meeting.mp3`) to test the
app locally. A 1–2 minute voice memo discussing a couple of decisions and
action items is enough to demo transcription + summarization.

This folder is git-ignored (except this file) so large/personal audio files
never get committed to the repo.
